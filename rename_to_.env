# Discord channel ID for bot messages - Find in Discord client (right-click channel, Copy ID)
CHANNEL_ID=<your_discord_channel_id>

# GPRDB data folder path -  The folder path to the GPRDB data files
DATA_FOLDER_PATH=/volume3/docker/gprdb

# Discord admin role ID - Find in Discord server settings (Roles, Copy ID)
DISCORD_ADMIN_ROLE_ID=<your_discord_admin_role_id>

# Discord bot token - Find in Discord Developer Portal (Bot tab, Token)
DISCORD_BOT_TOKEN=<your_discord_bot_token>

# Discord client ID - Find in Discord Developer Portal (General Information)
DISCORD_CLIENT_ID=<your_discord_client_id>

# Discord client secret - Find in Discord Developer Portal (General Information, Client Secret)
DISCORD_CLIENT_SECRET=<your_discord_client_secret>

# Discord guild ID (server ID) - Find in Discord client (right-click server icon, Copy ID)
DISCORD_GUILD_ID=<your_discord_guild_id>

# Discord public key - Find in Discord Developer Portal (General Information, Public Key)
DISCORD_PUBLIC_KEY=<your_discord_public_key>

# Discord redirect URI for OAuth2 -  Must match the URL for your /callback route (e.g., http://localhost:5000/callback)
DISCORD_REDIRECT_URI=<your_discord_redirect_uri>

# Flask secret key -  A random, complex string used to secure the Flask application
FLASK_SECRET_KEY=<your_flask_secret_key>

# PostgreSQL database name
POSTGRES_DB=<your_postgres_db_name>

# PostgreSQL host -  Typically 'localhost' or the name of the PostgreSQL service in Docker Compose
POSTGRES_HOST=<your_postgres_host>

# PostgreSQL password
POSTGRES_PASSWORD=<your_postgres_password>

# PostgreSQL port -  Usually 5432
POSTGRES_PORT=<your_postgres_port>

# PostgreSQL username
POSTGRES_USER=<your_postgres_user>

# Redis host -  Typically 'localhost' or the name of the Redis service in Docker Compose
REDIS_HOST=redis

# Redis port -  Usually 6379
REDIS_PORT=6379

# Webapp port -  The port the Flask webapp will run on
WEBAPP_PORT=<your_webapp_port>